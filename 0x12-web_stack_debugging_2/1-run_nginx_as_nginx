#!/usr/bin/env bash

# stop the nginx service if it's already running
adduser --system --no-create-home --shell /bin/false --group nginx
sudo nano /etc/nginx/nginx.conf
user nginx;
listen 8080 default_server;
sudo service nginx restart
